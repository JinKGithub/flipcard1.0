# 《翻翻对决》测试清单

本文档用于微信开发者工具、真机预览和发布前回归测试。每一轮测试建议记录设备型号、微信版本、网络环境、测试账号 openid、房间号和复现步骤。

## 1. 功能测试

### 1.1 登录与用户系统

- 首次启动游戏，检查 `wx.login` 和云函数登录是否返回 openid。
- 首次授权头像昵称，允许授权后主菜单显示微信昵称和头像。
- 拒绝授权后，主菜单显示默认玩家昵称和默认头像。
- 关闭并重新进入游戏，检查本地缓存用户信息是否恢复。
- 清理本地缓存后重新进入，检查是否重新走登录流程。
- 游客模式完成一局，检查本地战绩更新，云端敏感数据不异常写入。

### 1.2 主菜单

- Logo、用户信息、胜负统计、胜率显示正常。
- 创建房间按钮可进入房间等待页。
- 加入房间按钮可进入输入或加入流程。
- 单人练习可直接进入游戏对战页。
- 排行榜入口可进入排行榜页。
- 设置入口可切换音乐和音效开关，并写入本地存储。
- 背景粒子在低端机和小屏上数量降低，不影响按钮点击。

### 1.3 房间系统

- 创建房间后生成 6 位纯数字房间号。
- 房主进入房间后显示房主头像、昵称、准备状态。
- 复制房间号按钮可写入剪贴板。
- 分享房间按钮携带 `roomCode` 参数。
- 第二个玩家输入或通过分享参数加入房间。
- 满员房间不允许第三个玩家加入。
- 房主可切换难度，客人只能查看难度。
- 双方准备后显示 3 秒倒计时并进入游戏。
- 任一玩家取消准备，倒计时中断。
- 房间超过有效期后不能继续加入或开始。

### 1.4 游戏逻辑

- 初级生成 4x4 共 16 张卡牌，8 对。
- 中级生成 4x6 共 24 张卡牌，12 对。
- 高级生成 6x6 共 36 张卡牌，18 对。
- 点击第一张卡后保持翻开，必须翻第二张才判定。
- 不能连续点击同一张卡牌。
- 已匹配卡牌不可再次点击。
- 非己方回合不可翻牌。
- 匹配成功：当前玩家加 1 分，并继续回合。
- 匹配失败：两张卡延迟约 1 秒后翻回，并切换回合。
- 30 秒超时后，已翻开的未匹配卡翻回，并切换回合。
- 所有卡牌匹配后进入结算页。
- 分数高者胜利，分数相同显示平局。

### 1.5 实时同步

- 己方翻牌后，对手设备能看到同一卡牌翻开。
- 匹配成功后，双方分数和卡牌状态一致。
- 匹配失败后，双方翻回动画和回合状态一致。
- 当前回合、倒计时、分数在两台设备上保持一致。
- 对手断线重连后，能恢复最新房间和游戏状态。
- 云端状态冲突时，本地以云端数据为准并可继续操作。

### 1.6 分享

- 房间等待页分享卡片标题、图片和房间号参数正确。
- 好友点击邀请后能直接进入加入房间流程。
- 房间满员时显示明确提示。
- 结算页分享战绩可生成或使用分享图。
- 分享战绩参数不误触发加入房间。

### 1.7 排行榜

- 排行榜可查询 `ranks` 集合前 50 名。
- 按胜率排序正确。
- 按胜场排序正确。
- 当前用户在榜单中高亮。
- 无数据时显示空状态，不报错。
- 网络失败时显示失败提示，可返回主菜单。

## 2. 异常测试

### 2.1 网络异常

- 创建房间时断网，显示失败提示，不进入假房间。
- 加入房间时断网，保留输入状态并允许重试。
- 游戏中断网，暂停上传并尝试重连监听。
- 断网恢复后重新 watch 房间，游戏状态能同步。
- 云函数超时或失败时，界面不崩溃。

### 2.2 对手退出

- 房间等待页对手退出，房主看到空位恢复等待状态。
- 房间等待页房主退出，客人返回主菜单或提示房间解散。
- 游戏中对手退出，当前玩家胜利或收到对手离开提示。
- 结算保存时对手数据缺失，不影响本地结算展示。

### 2.3 快速点击与并发操作

- 快速点击同一张卡，只触发一次翻牌。
- 快速点击三张卡，第三张被忽略。
- 翻牌动画过程中点击其它卡，不破坏当前判定。
- 双方几乎同时操作时，只有当前回合玩家操作生效。
- 频繁点击按钮不会重复创建房间、重复准备或重复保存战绩。

### 2.4 边界条件

- 房间号为空、长度不足、非数字时不能加入。
- 房间不存在时显示提示。
- 房间已开始时不能加入。
- 用户昵称过长时 UI 省略显示，不溢出。
- 头像加载失败时显示默认头像。
- 卡片图片缺失时显示纯色占位或默认图，不影响游戏。
- 难度配置被异常修改为奇数卡牌数时，应阻止开始游戏或回退默认难度。

## 3. 兼容性测试

### 3.1 设备覆盖

- iPhone 小屏：iPhone SE / 8。
- iPhone 刘海屏：iPhone X / 12 / 14。
- Android 小屏低端机。
- Android 高刷或大屏机。
- 微信开发者工具模拟器。

### 3.2 屏幕与安全区域

- 竖屏下 Canvas 填满窗口。
- 刘海屏顶部内容不被遮挡。
- 底部安全区不遮挡退出按钮和主按钮。
- 4x4、4x6、6x6 网格在不同宽高比下居中显示。
- 高级 6x6 每张卡仍满足最小可点击尺寸。

### 3.3 微信版本

- 当前线上稳定版微信。
- 最近两个大版本微信。
- 微信开发者工具基础库最低支持版本。
- 不支持部分 API 时有降级逻辑，例如无开放数据域或无云开发时的提示。

## 4. 性能测试

### 4.1 启动速度

- 冷启动到 LoadingScene 可见时间。
- LoadingScene 到主菜单时间。
- 资源加载失败时是否跳过并继续。
- 首次进入和二次进入的缓存加载差异。

目标建议：

- 冷启动首屏小于 2 秒。
- 主菜单可交互小于 5 秒。
- 二次进入主菜单小于 2 秒。

### 4.2 流畅度

- 主菜单背景粒子保持 50-60 FPS。
- 翻牌动画无明显卡顿。
- 匹配粒子出现时不卡顿。
- 高级 6x6 场景点击响应及时。
- 低端机降低粒子数量后仍可正常反馈。

### 4.3 内存

- 连续进行 10 局游戏，内存不持续上涨。
- 切换场景后旧按钮、watch 监听、粒子被释放。
- 资源缓存可复用，不重复创建相同图片对象。
- 断线重连不会叠加多个 watch 监听。

### 4.4 网络与数据库

- 翻牌操作上传被节流，避免频繁写库。
- watch 监听关闭和重连行为正确。
- 房间结束后不再持续上传 gameState。
- 排行榜查询限制前 50 名，避免大范围扫描。

## 5. 临时资源说明

临时资源生成脚本位于：

```text
tools/generate-temp-assets.py
```

执行命令：

```powershell
python .\tools\generate-temp-assets.py
```

脚本会生成：

- `assets/images/cards/card-1.png` 到 `card-18.png`
- `assets/images/cards/back.png`
- `assets/images/ui/logo.png`
- `assets/images/ui/default-avatar.png`
- `assets/images/ui/button-primary.png`
- `assets/images/ui/button-secondary.png`
- `assets/images/ui/button-danger.png`
- `assets/images/share/invite.png`
- `assets/images/share/result-bg.png`

## 6. 后期替换真实 PNG 的方式

- 卡片正面图片保持命名：`card-1.png` 到 `card-18.png`。
- 卡背图片保持命名：`back.png`。
- 图片路径保持在：`assets/images/cards/`。
- UI 图片保持在：`assets/images/ui/`。
- 分享图保持在：`assets/images/share/`。
- 推荐尺寸：
  - 卡片正面：`256x256` PNG。
  - 卡背：`256x256` PNG。
  - Logo：透明背景 PNG，宽度建议 `512px`。
  - 分享图：`500x400` 或微信推荐分享比例。
- 替换图片后无需修改代码，只要文件名和路径不变即可。
- 若新增超过 18 种卡片图案，需要同步调整 `js/utils/config.js` 中的 `CARD_IMAGES` 数量。
