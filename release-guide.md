# 《翻翻对决》上线材料与发布检查清单

版本号：`1.0.0`

## 1. 小游戏基础信息

### 1.1 名称

翻翻对决

### 1.2 类目

休闲益智

### 1.3 简介

《翻翻对决》是一款双人记忆翻牌对战小游戏。玩家轮流翻开卡片，记住图案位置，成功配对即可得分并继续回合。游戏提供初级、中级、高级三种难度，支持好友房间对战、实时同步、战绩结算和排行榜，适合轻松休闲和记忆力挑战。

### 1.4 推荐短描述

双人实时翻牌对战，考验记忆力与策略判断。

### 1.5 推荐关键词

翻牌、记忆、双人对战、好友对战、休闲益智、小游戏、排行榜。

## 2. 视觉素材要求

### 2.1 小游戏图标

- 尺寸：`1024x1024`
- 格式：PNG
- 建议：
  - 图标主体清晰，避免细碎文字。
  - 可以使用两张翻开的卡片、闪光匹配效果、对战感元素。
  - 保持圆角裁切后仍可识别。
  - 不使用微信 Logo、官方标识或容易误导为官方产品的元素。

### 2.2 功能截图

需要准备 5 张功能截图，建议覆盖：

1. 主菜单：展示游戏 Logo、玩家信息、创建房间、加入房间、单人练习入口。
2. 房间等待：展示 6 位房间号、邀请好友、双方准备状态、难度选择。
3. 游戏对战：展示双方头像昵称、分数、倒计时、卡片网格。
4. 匹配效果：展示卡牌翻开、匹配成功或粒子反馈。
5. 结算页面：展示胜负结果、双方分数、分享战绩、返回主菜单。

截图建议：

- 使用真实或高质量临时卡片图，不要出现开发调试信息。
- 保持竖屏截图。
- 截图中不要出现微信开发者工具边框、控制台、调试浮层。
- 昵称和头像不要使用真实敏感信息。

## 3. 真实卡片图片说明

上线前建议替换当前临时纯色卡片为真实主题卡图。

### 3.1 图片数量

需要 18 种不同图案。

推荐主题：

- 动物主题：猫、狗、兔子、熊、狐狸、企鹅等。
- 水果主题：苹果、香蕉、草莓、葡萄、西瓜、橙子等。
- 表情主题：开心、惊讶、眨眼、酷、害羞、困困等。
- 美食主题：蛋糕、披萨、冰淇淋、汉堡、寿司、甜甜圈等。

### 3.2 图片规格

- 尺寸：`256x256`
- 格式：PNG
- 背景：建议透明或统一浅色背景。
- 风格：图案差异明显，避免颜色和轮廓过于相似。
- 文件大小：建议单张控制在 `50KB` 以内，优先压缩。

### 3.3 命名规则

```text
card-1.png
card-2.png
card-3.png
...
card-18.png
```

卡背：

```text
back.png
```

### 3.4 放置路径

```text
/assets/images/cards/
```

完整示例：

```text
/assets/images/cards/card-1.png
/assets/images/cards/card-2.png
/assets/images/cards/card-18.png
/assets/images/cards/back.png
```

### 3.5 替换方式

直接使用真实 PNG 覆盖同名文件即可。

只要保持路径和文件名不变，替换后无需修改代码。

## 4. 代码检查清单

### 4.1 移除调试输出

发布前检查并移除或替换源码中的调试输出。

当前扫描到源码中仍有：

```text
game.js: console.warn('wx.cloud is unavailable. Cloud features will be disabled.')
js/scenes/MenuScene.js: console.log(`${name} scene is not ready yet.`)
```

建议：

- `console.log` 必须移除。
- `console.warn` 可在开发环境保留，但正式版建议通过环境开关控制。
- 不要在控制台输出 openid、房间号、用户资料、云函数返回原始数据等敏感信息。

### 4.2 检查云环境 ID

当前配置中仍是占位值：

```text
js/utils/config.js
CLOUD_ENV_ID = 'your-cloud-env-id'
```

发布前必须替换为真实微信云开发环境 ID。

检查位置：

```text
js/utils/config.js
game.js
js/scenes/LoadingScene.js
```

### 4.3 检查 AppID

当前 `project.config.json` 中仍是：

```text
"appid": "touristappid"
```

发布前必须替换为真实小游戏 AppID。

### 4.4 域名与云开发配置

本项目主要使用微信云开发，仍需检查：

- 微信公众平台后台已开通云开发。
- 云函数已上传并部署：
  - `login`
  - `createRoom`
  - `joinRoom`
  - `leaveRoom`
  - `saveGameResult`
  - `cleanupRooms`
- 云数据库集合已创建：
  - `rooms`
  - `users`
  - `records`
  - `ranks`
- 数据库权限符合最小权限原则。
- 如后续使用外部图片、客服、统计、CDN 或 API，需要在小程序后台配置 request/downloadFile 合法域名。

### 4.5 资源检查

- `/assets/images/cards/` 下有 `card-1.png` 到 `card-18.png`。
- `/assets/images/cards/back.png` 存在。
- `/assets/audio/` 下有：
  - `flip.mp3`
  - `match.mp3`
  - `fail.mp3`
  - `click.mp3`
  - `victory.mp3`
  - `defeat.mp3`
  - `bgm.mp3` 如正式需要背景音乐。
- 分享图存在：
  - `/assets/images/share/invite.png`
  - `/assets/images/share/result-bg.png`
- 图标 `1024x1024` 已准备并上传到微信后台。

### 4.6 包体检查

- 压缩 PNG 图片。
- 删除无关设计源文件、测试导出图和临时说明文件。
- 确认 `project.config.json` 的忽略规则包含不需要打包的文档和工具文件。
- 真机预览检查首包大小。

### 4.7 隐私与数据检查

- 不采集手机号、定位、通讯录等非必要信息。
- 仅使用 openid、昵称、头像、战绩等游戏必要数据。
- 用户拒绝头像昵称授权时仍可游客模式体验。
- 战绩数据不展示敏感个人信息。

## 5. 审核注意事项

### 5.1 内容合规

- 游戏内容为休闲益智翻牌，不包含赌博、抽奖、现金收益、诱导充值等内容。
- 不使用侵犯版权的卡片图案、音乐、音效或品牌素材。
- 排行榜仅展示游戏战绩，不展示攻击性或违规文本。
- 用户昵称来自微信资料，需要做好超长文本和异常字符显示处理。

### 5.2 分享合规

- 分享文案不得诱导分享，例如“必须分享才能继续”“分享得奖励”等。
- 房间邀请分享应明确是好友对战邀请。
- 分享战绩应为用户主动触发，不自动弹出强制分享。

### 5.3 登录与授权

- 首次进入不要强制授权头像昵称。
- 拒绝授权后仍应允许继续体验。
- 授权按钮文案应明确用途，例如用于展示头像昵称和保存战绩。

### 5.4 排行榜与开放能力

- 好友排行榜若使用开放数据域，需要确保开放数据域代码和主域通信正常。
- 全服排行榜需要避免展示违规昵称时造成页面异常。
- 排行榜数据排序规则应真实，不虚构战绩。

### 5.5 云开发与安全

- 客户端不可直接写入不应由玩家控制的字段，例如胜负结果、排行榜核心统计应由云函数校验后更新。
- 房间号需要唯一性检查。
- 房间过期和异常房间需要定时清理。
- 中途退出、断线重连、对手离开需要有明确结果处理。

## 6. 发布前回归流程

1. 替换真实 AppID 和 `CLOUD_ENV_ID`。
2. 上传并部署所有云函数。
3. 创建并配置云数据库集合权限。
4. 替换真实卡片图、图标和功能截图。
5. 移除调试输出和无关测试代码。
6. 在微信开发者工具中执行编译、预览、真机调试。
7. 使用两台真机测试创建房间、加入房间、完整对战和结算。
8. 测试断网、退出、重连、快速点击。
9. 检查分享参数、排行榜、战绩保存。
10. 上传版本 `1.0.0`，填写版本说明并提交审核。

## 7. 版本说明建议

版本号：`1.0.0`

版本说明：

```text
首个正式版本上线：
1. 支持好友房间双人记忆翻牌对战。
2. 支持初级、中级、高级三种难度。
3. 支持实时分数、回合倒计时和结算分享。
4. 支持战绩统计和排行榜展示。
```

## 8. 上线后观察指标

- 新用户进入主菜单成功率。
- 创建房间成功率。
- 加入房间成功率。
- 对局完成率。
- 云函数错误率。
- 房间 watch 断线重连次数。
- 平均启动时长。
- 平均 FPS 或卡顿反馈。
- 用户分享点击率。
